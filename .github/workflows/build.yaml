name: Build the App

on: push

jobs:
  build:
    name: Build this App
    runs-on: ubuntu-latest
    steps:
      - name: Pull code
        uses: actions/checkout@v2
      - name: Print current directory
        run: pwd
      - name: List files in Dir
        run: ls